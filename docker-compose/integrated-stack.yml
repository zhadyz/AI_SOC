# Compatibility entry point for the supported research platform.
# Wazuh/TheHive/Shuffle infrastructure is an optional, separately managed lab.
include:
  - path: ../compose.yaml
    env_file: ../.env
